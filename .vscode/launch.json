{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Transformer",
            "type": "debugpy",
            "request": "launch",
            "module": "src.analysis.transform.transformer",
            "console": "integratedTerminal",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}